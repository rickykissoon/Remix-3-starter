export type StyleProperties = {
    [key in keyof Omit<CSSStyleDeclaration, 'item' | 'setProperty' | 'removeProperty' | 'getPropertyValue' | 'getPropertyPriority'>]?: string | number | null | undefined;
} & {
    [key in `--${string}`]?: string | number | null | undefined;
};
export interface EnhancedStyleProperties extends StyleProperties {
    [key: string]: string | number | null | EnhancedStyleProperties | undefined;
}
export declare function processStyle(styleObj: EnhancedStyleProperties, styleCache: Map<string, {
    className: string;
    css: string;
}>): {
    className: string;
    css: string;
};
export declare function clearStyleCache(styleCache: Map<string, {
    className: string;
    css: string;
}>): void;
