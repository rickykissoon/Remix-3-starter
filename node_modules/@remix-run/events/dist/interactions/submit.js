import { dom } from "../lib/targets.js";
/**
 * Submit helper for forms that prevents default browser submission
 * and passes a FormData object to the provided action function.
 *
 * TODO: maybe we don't need a convenience API here? LLMs can crank out this boilerplate no problem
 */
export let submit = (action) => {
    return dom.submit((event) => {
        event.preventDefault();
        let form = event.currentTarget;
        let formData = new FormData(form);
        action(formData);
    });
};
//# sourceMappingURL=submit.js.map